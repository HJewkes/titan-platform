# @titan-design/__NAME__

__DESCRIPTION__

Tier __TIER__ of the titan-platform DAG. May import only packages in the same tier or
below; the `package-layers` rule in `.codewatch/check.json` enforces this in CI.

Status: placeholder. Tracked by __TASK__.
