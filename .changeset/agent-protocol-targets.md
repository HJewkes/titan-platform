---
"@titan-design/agent-protocol": minor
---

agent-protocol: add `fork` and `handoff` execution targets, `HandoffIdentity`, and an optional `pinnedNativeId` on `fresh` that `prepare` records as the execution's conversation (TP-192 S2).
