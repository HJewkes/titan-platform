---
"@titan-design/retrieval-eval": patch
---

Add the `served` arm: parses rendered bootstrap and spawn context blocks from transcripts and reports served, opened, opened-section and cited per class, initiative and file, with the unserved base rate (TP-330).
