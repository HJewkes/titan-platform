---
"@titan-design/style-checker": minor
---

Add `AUDIT_RUFF_RULES`, `generateRuffAuditConfig` and `runRuffAudit`, a pinned ruff rule set run with preview on and repo-relative paths. `CheckDiagnostic` gains `endLine`, filled from ruff's `end_location`.
