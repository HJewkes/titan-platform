---
"@titan-design/agent-protocol": minor
---

agent-protocol: add immutable namespaced `correlations` on `prepare` and `ExecutionRecord`, with `CORRELATION_KEY_PATTERN`, `RESERVED_CORRELATION_PREFIXES` (`broker`, `agent-thread`), `correlationKey` and `validateCorrelations` (TP-192 S3).
