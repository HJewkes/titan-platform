# @titan-design/react-app

React hooks over @titan-design/rpc-client and a Vite preset for daemon-backed apps

Tier ui of the titan-platform DAG. May import only packages in the same tier or
below; the `package-layers` rule in `.codewatch/check.json` enforces this in CI.

Status: placeholder. Tracked by TP-140.
