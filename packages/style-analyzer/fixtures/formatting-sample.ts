const name = "hello";
const greeting = "world";
const template = `hello ${name}`;

const obj = {
  a: 1,
  b: 2,
};

function foo(
  param1: string,
  param2: number,
) {
  if (true) {
    return param1;
  } else {
    return param2;
  }
}
